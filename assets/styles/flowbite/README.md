# Flowbite stylesheet

Vendored precompiled CSS from Flowbite **3.1.2** (MIT; see LICENSE.md).
Source: https://cdn.jsdelivr.net/npm/flowbite@3.1.2/dist/flowbite.min.css

Keep this version aligned with `flowbite` in `importmap.php`. Download the
stylesheet and license from the same release when upgrading, then verify the
component preview. Custom styles belong in `../app.css`; no Tailwind compiler
is required. JavaScript dependencies are restored with `importmap:install`.
